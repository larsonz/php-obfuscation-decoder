# php-obfuscation-decoder
production-grade version of  tool designed for real-world PHP obfuscation analysis. It is binary-safe, decodes all payloads, supports multiple quoting styles, performs basic heuristics, and produces separate outputs per payload.
